# examples/ (placeholder)

Meant to hold example sentences annotated with construction spans, in the
CoNLL-Uc sense described in `../../publications/tutorial/guida.md`
(`CXN=<cxn_id>:<slot_id>` in MISC, aligned against a `.conllc` formalization).
This doesn't exist yet — the closest current material is
`data/db_esempi/` (plain UD-parsed example sentences, not yet annotated with
construction spans) and `data/examples.conllu`.
